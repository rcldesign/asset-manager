# Example environment configuration for DumbAssets Enhanced
# Copy this file to .env and update with your values

# Application Environment
NODE_ENV=development
PORT=3001

# Database Configuration
# For embedded database (default)
USE_EMBEDDED_DB=true

# For external PostgreSQL (uncomment and set to use)
# USE_EMBEDDED_DB=false
# DATABASE_URL=postgresql://user:password@localhost:5432/dumbassets_enhanced

# Redis Configuration
# For embedded Redis (default)
USE_EMBEDDED_REDIS=true

# For external Redis (uncomment and set to use)
# USE_EMBEDDED_REDIS=false
# REDIS_URL=redis://localhost:6379

# Security - MUST CHANGE IN PRODUCTION
JWT_SECRET=your-jwt-secret-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production
SESSION_SECRET=your-session-secret-change-in-production
ENCRYPTION_KEY=your-32-byte-encryption-key-change

# Application URLs
BASE_URL=http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# File Storage
UPLOAD_DIR=/app/uploads
FILE_STORAGE_TYPE=local

# SMB/CIFS Configuration (optional)
# SMB_SHARE=//server/share
# SMB_USERNAME=
# SMB_PASSWORD=
# SMB_DOMAIN=

# Email Configuration (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@dumbassets.local

# OIDC Configuration (optional)
# OIDC_ISSUER_URL=https://your-oidc-provider.com
# OIDC_CLIENT_ID=
# OIDC_CLIENT_SECRET=
# OIDC_REDIRECT_URI=http://localhost:3001/auth/oidc/callback

# Google Calendar Integration (optional)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Apprise Notifications (optional)
# APPRISE_URL=

# Docker Volume Paths (for production)
# DATA_PATH=./data
# REDIS_PATH=./redis
# UPLOADS_PATH=./uploads